<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>cn.pixel.boot</groupId>
        <artifactId>pingdou</artifactId>
        <version>${revision}</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>
    <artifactId>pingdou-module-hrm</artifactId>
    <packaging>jar</packaging>

    <name>${project.artifactId}</name>
    <description>
        hrm 包下，人力资源管理（Human Resource Management）
        例如说：员工、招聘、考勤、薪资、社保、绩效等等
    </description>

    <dependencies>
        <dependency>
            <groupId>cn.pixel.boot</groupId>
            <artifactId>pingdou-module-system</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>cn.pixel.boot</groupId>
            <artifactId>pingdou-module-bpm</artifactId>
            <version>${revision}</version>
        </dependency>

        <!-- Web 相关 -->
        <dependency>
            <groupId>cn.pixel.boot</groupId>
            <artifactId>pingdou-spring-boot-starter-web</artifactId>
        </dependency>

        <dependency>
            <groupId>cn.pixel.boot</groupId>
            <artifactId>pingdou-spring-boot-starter-security</artifactId>
        </dependency>

        <!-- DB 相关 -->
        <dependency>
            <groupId>cn.pixel.boot</groupId>
            <artifactId>pingdou-spring-boot-starter-mybatis</artifactId>
        </dependency>

        <!-- 工具类相关 -->
        <dependency>
            <groupId>cn.pixel.boot</groupId>
            <artifactId>pingdou-spring-boot-starter-excel</artifactId>
        </dependency>

        <!-- 定时任务相关 -->
        <dependency>
            <groupId>cn.pixel.boot</groupId>
            <artifactId>pingdou-spring-boot-starter-job</artifactId>
        </dependency>

        <!-- Test 测试相关 -->
        <dependency>
            <groupId>cn.pixel.boot</groupId>
            <artifactId>pingdou-spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

</project>
